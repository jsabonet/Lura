# Variáveis de Ambiente para Produção
# Copie este arquivo para .env na produção

# Database
POSTGRES_PASSWORD=SuaSenhaPostgresMuitoSegura123!

# Django Settings
DEBUG=False
SECRET_KEY=sua-chave-secreta-super-segura-aqui-com-50-caracteres-minimo
ALLOWED_HOSTS=localhost,127.0.0.1,seudominio.com,*.seudominio.com

# Database URL (auto-gerada a partir do POSTGRES_PASSWORD)
DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@db:5432/agroalerta

# APIs Externas
OPENWEATHER_API_KEY=sua-chave-openweather
GOOGLE_MAPS_API_KEY=sua-chave-google-maps

# Email (opcional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=seu-email@gmail.com
EMAIL_HOST_PASSWORD=sua-senha-app

# Segurança
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://your-domain.com
CSRF_TRUSTED_ORIGINS=http://localhost:3000,https://your-domain.com

# Redis (para cache - opcional)
REDIS_URL=redis://redis:6379/0
