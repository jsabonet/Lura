# Variáveis de Ambiente para Produção
# Copie este arquivo para .env na produção

# Database
POSTGRES_DB=lurafarm
POSTGRES_USER=postgres
POSTGRES_PASSWORD=SuaSenhaPostgresMuitoSegura123!

# Django Settings
DEBUG=False
SECRET_KEY=sua-chave-secreta-super-segura-aqui-com-50-caracteres-minimo
ALLOWED_HOSTS=localhost,127.0.0.1,68.183.211.15,seudominio.com,*.seudominio.com

# Database URL (auto-gerada a partir das variáveis acima)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}

# APIs Externas (Frontend)
NEXT_PUBLIC_OPENWEATHER_API_KEY=
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# APIs Externas (Backend opcional)
OPENWEATHER_API_KEY=
GOOGLE_MAPS_API_KEY=

# Email (opcional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=seu-email@gmail.com
EMAIL_HOST_PASSWORD=sua-senha-app

# Segurança
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://68.183.211.15
CSRF_TRUSTED_ORIGINS=http://localhost,http://127.0.0.1,http://68.183.211.15

# Redis (para cache - opcional)
REDIS_URL=redis://redis:6379/0

# Twilio (opcional)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
