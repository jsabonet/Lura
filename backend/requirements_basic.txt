# Requirements mínimos - apenas dependências em uso
# Core Django
Django==4.2.7
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.0
django-cors-headers==4.3.1

# Base de dados
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# Servidor de produção
gunicorn==21.2.0
whitenoise==6.6.0

# APIs externas (usado no clima/views.py)
requests==2.31.0

# Processamento de imagens (usado em pragas/models.py)
Pillow==10.0.0

# Variáveis de ambiente
python-dotenv==1.0.0

# Utilitários básicos (usado em vários lugares)
python-dateutil==2.8.2
